{
  "name": "rideau-canal-dashboard",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"node server.js\"",
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node server.js"
  },
  "dependencies": {
    "@azure/cosmos": "^4.9.2",
    "concurrently": "^9.2.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
